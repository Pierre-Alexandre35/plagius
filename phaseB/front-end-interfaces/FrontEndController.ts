/**
 * This interface will control the logic on the front end UI
 */
export interface FrontEndController {

    filesToJSON();

    presentPercentage();

    PresentDiff();

    ProcessFiles_setOne();

    ProcessFiles_setTwo();
}
